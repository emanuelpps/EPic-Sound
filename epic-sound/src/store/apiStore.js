/* import { create } from 'zustand';
import trendingTracks from '@/services/trendingTracks';

export const apiStore = create((set) => ({
  getTrendingTracks: () => set((state) => (trendingTracks())),
  getAlbumWeek: () => set((state) => ({})),
  getPopularTracks: () => set((state) => ({})),
}))
 */