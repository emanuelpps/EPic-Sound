/* "use client";
import { useTrackStore } from "../../store/trackStore";

export default function trackHandler(track) {
  const setTrack = useTrackStore(state => state.setTrack);
  setTrack(track);
  console.log("track", track);
}

 */